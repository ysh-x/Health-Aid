CREATE TABLE BMIREPORT
(
     USERNAME VARCHAR(40) NOT NULL,
     AGE INT,
     HEIGHT FLOAT,
     WEIGHT FLOAT,
     BMI FLOAT,
     RESULTS VARCHAR(40)
);

GENERAL

CREATE TABLE LOGIN
(
     NAME VARCHAR(40),
     USERNAME VARCHAR(40) PRIMARY KEY NOT NULL,
     PASSWORD VARCHAR(40),
     PIN VARCHAR(5)
);
